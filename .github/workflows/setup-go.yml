name: Setup go

on:
  workflow_call:

jobs:
  set-up:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
